/**
 * HANDLERS — EXECUTIVE + ADAPTIVE RESPONSE STRATEGIES
 * ====================================================
 *
 * STUB FILE — populated in Step 1 of the sprint.
 *
 * this file will contain:
 * - ExecutiveHandler abstract class (what to DO when executive help-seeking is detected)
 * - AdaptiveHandler abstract class (what to DO when adaptive help-seeking is detected)
 * - DefaultExecutiveHandler — returns a show_panel instruction
 * - SilentAdaptiveHandler — returns silent (current behavior)
 * - getActiveExecutiveHandler() — swap point for executive interventions
 * - getActiveAdaptiveHandler() — swap point for adaptive interventions
 *
 * see src/shared/types.ts for the ExecutiveIntervention and AdaptiveIntervention types
 * that handlers will return once this is implemented.
 */

export {}; // placeholder so TS doesn't complain about an empty module
